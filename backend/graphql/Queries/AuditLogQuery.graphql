extend type Query {
    auditLogs(store_id: ID!, page: Int, per_page: Int): AuditLogPage!
        @guard(with: ["sanctum"])
        @field(resolver: "App\\GraphQL\\Queries\\AuditLogResolver@index")
}
