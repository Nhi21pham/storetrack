extend type Mutation {
    login(
        email: String!
        password: String!
    ): LoginResponse! 
    @field(resolver: "App\\GraphQL\\Mutations\\LoginResolver@login")
}

type LoginResponse {
    message: String!
    token: String!
    user: User!
}